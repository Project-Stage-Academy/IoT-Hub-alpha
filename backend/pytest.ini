[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py

pythonpath = . apps

addopts = --reuse-db --cov=apps --cov-report=xml --cov-report=term-missing --cov-fail-under=80

markers = smoke: critical smoke tests
